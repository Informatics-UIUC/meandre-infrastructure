#
# ant library
#
ant.lib.version=1.7
ant.lib.dir=${lib.dir}/Jetty

#
# apache-commons: upload io
# http://commons.apache.org/
#
apache-commons-upload.version=1.2
apache-commons-upload.dir=${lib.dir}/Commons-Upload
apache-commons-upload.jar=${apache-commons-upload.dir}/commons-fileupload-${apache-commons-upload.version}.jar

apache-commons-io.version=1.3.2
apache-commons-io.dir=${lib.dir}/Commons-Upload
apache-commons-io.jar=${apache-commons-io.dir}/commons-io-${apache-commons-io.version}.jar

apache-commons-logging.version=1.1
apache-commons-logging.dir=${lib.dir}/JENA
apache-commons-logging.jar=${apache-commons-logging.dir}/commons-logging-${apache-commons-logging.version}.jar

#
# apache-derby
# http://db.apache.org/derby
#
apache-derby.version=10.3.1.4
apache-derby.dir=${lib.dir}/Derby

#
# Lucene
#
lucene.version=2.0.0
lucene.dir=${lib.dir}/JENA

#
# jena semantic web framework
# http://jena.sourceforge.net/downloads.html
#
jena.version=0.0
jena.dir=${lib.dir}/JENA

#
# Jetty  servlet container
# www.mortbay.org/
#
jetty.version=6.1.5
jetty.dir=${lib.dir}/Jetty

#
# junit jar library
#
junit.version=4.4
junit.dir=${lib.dir}/junit
junit.jar=${junit.dir}/junit-${junit.version}.jar

#
# saxon XSL transformer
#
saxon.version=8
saxon.dir=${lib.dir}/Saxon

#
# Jython (Python interpreter for Java)
# www.jython.org/
#
jython.version=2.2.1
jython.dir=${lib.dir}/Jython-${jython.version}
jython.jar=${jython.dir}/jython.jar

#
# svnant
# http://subclipse.tigris.org/svnant.html
# Used to find out the current build and append that to the
# build artifacts
#
svnant.version=1.1.0-rc2
svnant.dir=${lib.dir}/svnant-${svnant.version}

#
# plugins
# custom servlets that are specific to a project go here
#
truezip.version=6.6
truezip.dir=${lib.dir}/truezip-${truezip.version}
truezip.jar=${truezip.dir}/truezip-${truezip.version}.jar

#
# xstream
#
xstream.dir=${lib.dir}/XStream

#
# httpUnit test libraries
# http://httpunit.sourceforge.net/
#
httpunit.version=1.6.2
httpunit.dir=${lib.dir}/httpunit-${httpunit.version}
httpunit.jar=${httpunit.dir}/httpunit-${httpunit.version}.jar

#
# jtidy [required by httpunit]
#
jtidy.version=04aug2007r7
jtidy.dir=${lib.dir}/jtidy-${jtidy.version}

#
# nekohtml [required by httpunit]
#
nekohtml.version=0.9.1
nekohtml.dir=${lib.dir}/nekohtml-${nekohtml.version}

#
#rhino js interpreter [ required by httpunit]
#
rhino.version=1.5R4.1
rhino.dir=${lib.dir}/rhino-${rhino.version}

<!-- DO NOT EDIT BELOW THIS LINE PLEASE -->
<!-- vim:sw=4:softtabstop=4:expandtab
-->
